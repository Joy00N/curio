// Development environment
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3000/api', // Change to your backend URL
  useMockGeneration: true, // Set to false when API is ready
  appVersion: '1.0.0'
};
